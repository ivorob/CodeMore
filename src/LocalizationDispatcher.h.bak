#pragma once

#include <QTranslator>
#include <QLocale>
#include <QObject>

class LocalizationDispatcher : public QObject {
    Q_OBJECT
public:
    LocalizationDispatcher(const QLocale& locale, QObject *parent = nullptr);
//private:
    //QStringList fillLanguages(const QString& prefix, const QString& directory);
    //void translateApp(QTranslator *translator, const QString& languageToTranslate);
private:
    QTranslator *translator;
}
